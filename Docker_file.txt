FROM ruby:3.2

RUN apt-get update -qq && apt-get install -y nodejs postgresql-client

WORKDIR /app
COPY . /app

RUN gem install rails -v 7.0.4 && bundle install

CMD ["rails", "server", "-b", "0.0.0.0"]